<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.group.projectga">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.SplashScreen">
            <intent-filter android:label="string/filter_title_splash">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Accepts URIs that begin with http://www.example.com/splash" -->
                <data android:scheme="http"
                      android:host="www.example.com"/>
                <!-- Accepts URIs that begin with "example://splash" -->


            </intent-filter>

        </activity>


        <activity android:name=".activities.SignInActivity" />
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.ContactDetailsActivity" />
        <activity android:name=".activities.AddGuardianActivity" />
        <activity android:name=".activities.MainMenuActivity" />
        <!--<activity android:name=".activities.ForgetPasswordActivity" />
        <activity android:name=".activities.ResetPassword"></activity> -->
    </application>

</manifest>